query listAds ($orderBy: AdOrderByInput, $first: Int){
  ads (orderBy: $orderBy, first: $first) {
    id
    title
    description
    image
    price
    ranking
    host {
      fullName
    }
    createdAt
  }
}